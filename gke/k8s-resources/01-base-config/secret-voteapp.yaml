apiVersion: v1
kind: Secret
metadata:
  name: voteapp-secret
  namespace: vote-app
type: Opaque
stringData:
  # Keeping MYSQL_PASSWORD name for backward compatibility with app code
  MYSQL_PASSWORD: "${DB_PASSWORD}"
